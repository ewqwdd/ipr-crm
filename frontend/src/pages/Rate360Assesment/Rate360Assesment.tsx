// import { rate360Api } from "@/shared/api/rate360Api"
// import { skillsApi } from "@/shared/api/skillsApi"
// import { universalApi } from "@/shared/api/universalApi"
// import { useParams } from "react-router"

export default function Rate360Assesment() {

    // const {id} = useParams()
    // const {data, isFetching} = rate360Api.useAssignedRatesQuery()
    // const {data: skills, isFetching: skillsFetching} = skillsApi.useGetSkillsQuery()
    // const {data: specs, isFetching: specsFetching} = universalApi.useGetSpecsQuery()
    // if (!id) return null

    // const found = data?.find((item) => item.id === parseInt(id))
    // const foundSpec = specs?.find((item) => item.id === found?.specId)

    // if (!found) return null

  return (
    <div>Rate360Assesment</div>
  )
}
